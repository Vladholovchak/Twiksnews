<%= bootstrap_form_for(@news_source) do |form| %>
  <% if @news_source.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@news_source.errors.count, "error") %> prohibited
        this source from being saved:
      </h2>
      <ul>
        <% @news_source.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  </p>
  <%= form.text_field :name %>
  </p>
  <%= form.submit %>
  </p>
<% end %>